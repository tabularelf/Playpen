// feather ignore all
/// @desc This returns the next file found as per the previous PlaypenFileFindFirst() call. Or returns an empty string if no more files.
/// @returns {String} 
function PlaypenFileFindNext(_filepath, _attribute) {
	return file_find_next();
}